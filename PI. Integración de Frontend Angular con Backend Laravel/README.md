# Catálogo de Películas — Angular (Frontend) + Laravel (backend)
contiene un frontend en Angular y un backend en Laravel con un CRUD completo para un catálogo de películas.

## Estructura principal


- `frontend/` — aplicación Angular
- `backend/`  — aplicación Laravel 
